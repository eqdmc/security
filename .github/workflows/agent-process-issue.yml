# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: "Agent: Process Issue"

on:
  issues:
    types: [labeled]

permissions:
  issues: write
  contents: read

jobs:
  process:
    if: github.event.label.name == 'agent-work'
    uses: eqdmc/.github/.github/workflows/_agent-process-issue.yml@fc51fa3ceba543032cbd16547cb849dafc8eb5f8 # v0.2.1
    with:
      issue-number: ${{ github.event.issue.number }}
    secrets: inherit
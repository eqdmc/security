# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: PR Check

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, labeled, unlabeled]

permissions:
  contents: write
  pull-requests: write
  issues: read

jobs:
  pr-metadata:
    uses: eqdmc/.github/.github/workflows/pr-metadata-check.yml@fc51fa3ceba543032cbd16547cb849dafc8eb5f8 # v0.2.1
    secrets: inherit

  auto-label:
    uses: eqdmc/.github/.github/workflows/auto-label.yml@fc51fa3ceba543032cbd16547cb849dafc8eb5f8 # v0.2.1
    secrets: inherit

  deps-vetting:
    uses: eqdmc/.github/.github/workflows/deps-vetting.yml@fc51fa3ceba543032cbd16547cb849dafc8eb5f8 # v0.2.1
    secrets: inherit
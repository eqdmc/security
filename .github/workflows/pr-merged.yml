# Managed by eqdmc/.github — do not edit. Changes overwritten on next sync.
name: PR Merged

on:
  pull_request_target:
    types: [closed]

permissions:
  pull-requests: write
  contents: read

jobs:
  outcome:
    uses: eqdmc/.github/.github/workflows/outcome-comment.yml@fc51fa3ceba543032cbd16547cb849dafc8eb5f8 # v0.2.1
    secrets: inherit

  update-queued:
    uses: eqdmc/.github/.github/workflows/auto-update-branch.yml@fc51fa3ceba543032cbd16547cb849dafc8eb5f8 # v0.2.1
    secrets: inherit
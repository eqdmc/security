# CI — produces "ci / ci" check via reusable workflow to satisfy org ruleset.
name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  merge_group:

jobs:
  ci:
    uses: eqdmc/.github/.github/workflows/ci.yml@main
    secrets: inherit

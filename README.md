# repo-name

> Created from [eqdmc/repo-template](https://github.com/eqdmc/repo-template). Governance managed by [eqdmc/.github](https://github.com/eqdmc/.github).

## Getting started

1. Replace `repo-name` in this README
2. Add repo-specific CI steps to `.github/workflows/ci.yml`
3. Configure `dependabot.yml` with your package ecosystems

## Governance

All workflow files in `.github/workflows/` are synced from the org SSoT every 6 hours. Do not edit managed workflows directly — changes will be overwritten.

Custom CI (`ci.yml`) is yours to modify — it will not be overwritten once present.